# Project Rules

1. Do each numbered task one at a time — complete and verify before moving to the next.
2. Never write a file longer than ~150 lines in a single tool call; split large files into multiple sequential Edit/Write passes.
3. Start a fresh session if the conversation reaches 20+ tool calls to avoid API errors.
